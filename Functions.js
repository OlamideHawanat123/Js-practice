const prompt = require("prompt-sync")();

console.log(types("Sapa size", "Small money", "Big boys", "Odogwu"))


let userChoice = prompt("Choose from the above options:")


function types(pizzaTypes){
return pizzaTypes
}

function slice(numberOfSlice){
return numberOfSlice
}