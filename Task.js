const human = {
    firstName: 'Raheem',
    lastName: 'Hawanat'
}

    function fullName(name){
    let theFullName = human;
    return theFullName
}

fullName(human)

 let arrKeys = Object.keys(car);
 for(let i = 0; i < arrKeys.length; i++) {
  console.log(arrKeys[i] + ": " + car[arrKeys[i]])