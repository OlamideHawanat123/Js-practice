for(let count = 0; count < 7; count++){
if(count >= 0)console.log("#")
if(count >= 1)console.log("#")

if(count >= 2)console.log("#" + "#")

if(count >= 3)console.log("#")

if(count >= 4)console.log("#" + "#" + "#")

if(count >= 5)console.log("#")
if(count >= 6)console.log("#")

if(count >= 7)console.log("#")
}


